{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Entry Management System Frontend - Static Version for Netlify",
  "scripts": {
    "build": "node build-config.js",
    "start": "npm run build && echo 'Config built from .env'",
    "dev": "npm run build && echo 'Development ready'"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}